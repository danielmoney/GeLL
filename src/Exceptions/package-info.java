/**
 * Provides exceptions that are common across the package.  These are a general
 * exception that all other package exception extend, input/output exceptions
 * and an error that is thrown when something happens that we thought couldn't.
 */
package Exceptions;
